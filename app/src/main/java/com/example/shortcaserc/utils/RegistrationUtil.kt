package com.example.shortcaserc.utils

import com.example.shortcaserc.database.ComicObject

object RegistrationUtil {

    /*
    * The comic is not valid if the response body is empty,
    * of if the request fails.
    */

    fun validateComicObject(comicObject: ComicObject): Boolean {
        return true
    }

}